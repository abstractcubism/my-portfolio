// Tailwind v4 uses ESM config. Keep it minimal so it's picked up.
export default {
  darkMode: 'class',
  theme: {
    extend: {
      fontFamily: {
        sans: ["Inter", "Helvetica", "Arial", "sans-serif"],
      },
    },
  },
};
